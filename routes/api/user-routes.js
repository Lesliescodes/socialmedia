const { Router } = require('express');
const { Thoughts, User } = require('../models');

const {
    getAllUser,
    getSingleUser
    = require('../../controllers/User-controllers');
}

.route('/')
 